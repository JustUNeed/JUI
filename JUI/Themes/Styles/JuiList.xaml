<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:jui="clr-namespace:JUI.Controls">

    <jui:DoubleToThicknessConverter x:Key="Jui.SpacingToMargin"/>

    <Style TargetType="{x:Type jui:JuiList}">
        <Setter Property="Background" Value="{DynamicResource Jui.Surface.Window}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Jui.Border}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

        <!-- 纵向单列虚拟化面板 -->
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type jui:JuiList}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <ScrollViewer x:Name="PART_Scroll" Focusable="False"
                                          Padding="{TemplateBinding Padding}"
                                          HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                          VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                          CanContentScroll="True">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </ScrollViewer>

                            <ContentPresenter x:Name="PART_Empty"
                                              TextElement.Foreground="{DynamicResource Jui.Text.Secondary}"
                                              Content="{TemplateBinding EmptyContent}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="PART_Empty" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <!-- 项容器样式: 铺满整行, 垂直间距取 ItemSpacing, 拖放/选中态高亮 -->
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="Margin"
                            Value="{Binding ItemSpacing,
                                            RelativeSource={RelativeSource AncestorType={x:Type jui:JuiList}},
                                            Converter={StaticResource Jui.SpacingToMargin}}"/>
                    <Setter Property="Height"
                            Value="{Binding ItemHeight, RelativeSource={RelativeSource AncestorType={x:Type jui:JuiList}}}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <Grid>
                                    <Border x:Name="ClipBorder"
                                            CornerRadius="{Binding ItemCornerRadius, RelativeSource={RelativeSource AncestorType={x:Type jui:JuiList}}}"
                                            Background="Transparent"
                                            SnapsToDevicePixels="True">
                                        <Border.OpacityMask>
                                            <VisualBrush>
                                                <VisualBrush.Visual>
                                                    <Border Background="White"
                                                            CornerRadius="{Binding ItemCornerRadius, RelativeSource={RelativeSource AncestorType={x:Type jui:JuiList}}}"
                                                            Width="{Binding ActualWidth, ElementName=ClipBorder}"
                                                            Height="{Binding ActualHeight, ElementName=ClipBorder}"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Border.OpacityMask>
                                        <ContentPresenter VerticalAlignment="Center"
                                                          HorizontalAlignment="Stretch"/>
                                    </Border>

                                    <Border x:Name="Bd"
                                            CornerRadius="{Binding ItemCornerRadius, RelativeSource={RelativeSource AncestorType={x:Type jui:JuiList}}}"
                                            BorderBrush="Transparent" BorderThickness="0"
                                            Background="Transparent"
                                            IsHitTestVisible="False"
                                            SnapsToDevicePixels="True"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#22000000"/>
                                    </Trigger>
                                    <Trigger Property="jui:JuiList.IsDropTarget" Value="True">
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Jui.Accent}"/>
                                        <Setter TargetName="Bd" Property="Background" Value="#330078D4"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Jui.Accent}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
