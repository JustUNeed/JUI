<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:JUI.Controls">

    <Style TargetType="{x:Type controls:JuiToastControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:JuiToastControl}">
                    <Border Background="{DynamicResource Jui.Surface.Default}"
                            BorderBrush="{DynamicResource Jui.Border}"
                            BorderThickness="1"
                            CornerRadius="0"
                          
                            SnapsToDevicePixels="True">
                        <TextBlock Text="{TemplateBinding Message}"
                                   Foreground="{DynamicResource Jui.Text.Primary}"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   MaxWidth="280"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
