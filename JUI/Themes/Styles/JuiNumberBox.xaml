<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:JUI.Controls">

    <Style TargetType="{x:Type controls:JuiNumberBox}">
        <Setter Property="Background" Value="{DynamicResource Jui.Surface.Default}"/>
        <Setter Property="Foreground" Value="{DynamicResource Jui.Text.Primary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Jui.Border}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="MinWidth" Value="60"/>
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:JuiNumberBox}">
                    <Border x:Name="OuterBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Cursor="SizeWE"
                            SnapsToDevicePixels="True">
                        <Grid Margin="{TemplateBinding Padding}">
                            <!-- 显示态: Label 靠左, 数值+单位 靠右 -->
                            <DockPanel x:Name="DisplayPanel" LastChildFill="False">
                                <TextBlock x:Name="LabelText"
                                           DockPanel.Dock="Left"
                                           Text="{TemplateBinding Label}"
                                           Foreground="{DynamicResource Jui.Text.Secondary}"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"/>
                                <TextBlock x:Name="ValueText"
                                           DockPanel.Dock="Right"
                                           Text="{TemplateBinding ValueDisplayText}"
                                           Foreground="{TemplateBinding Foreground}"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"/>
                            </DockPanel>

                            <!-- 编辑态: 复用 JuiTextBox, 无边框无内距, 右对齐 -->
                            <controls:JuiTextBox x:Name="PART_TextBox"
                                                 BorderThickness="0"
                                                 Padding="0"
                                                 Background="Transparent"
                                                 CornerRadius="0"
                                                 FontSize="{TemplateBinding FontSize}"
                                                 VerticalContentAlignment="Center"
                                                 HorizontalContentAlignment="Right"
                                                 Visibility="Collapsed"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush"
                                    Value="{DynamicResource Jui.Accent.Hover}"/>
                            <Setter TargetName="OuterBorder" Property="Background"
                                    Value="{DynamicResource Jui.Surface.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsEditing" Value="True">
                            <Setter TargetName="DisplayPanel" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_TextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="OuterBorder" Property="Cursor" Value="IBeam"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="OuterBorder" Property="Background"
                                    Value="{DynamicResource Jui.Surface.Active}"/>
                            <Setter TargetName="OuterBorder" Property="Cursor" Value="Arrow"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Jui.Text.Disabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
